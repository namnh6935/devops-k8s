apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins
  namespace: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins
  template:
    metadata:
      labels:
        app: jenkins
    spec:
      containers:
      - name: jenkins
        image: jenkins/jenkins:lts-jdk17
        ports:
          - name: http-port
            containerPort: 8080
          - name: jnlp-port
            containerPort: 50000
        volumeMounts:
          - name: jenkins
            mountPath: /var/jenkins_home
      volumes:
      - name: jenkins
        persistentVolumeClaim:
          claimName: api-jenkins

---
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: training-jenkins-ingress
#   namespace: jenkins
# spec:
#   ingressClassName: cilium
#   rules:
#   - host: "jenkins.nam.com"
#     http:
#       paths:
#       - backend:
#           service:
#             name: jenkins
#             port:
#               number: 8080
#         path: /
#         pathType: Prefix
